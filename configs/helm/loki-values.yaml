persistence:
  enabled: true
  accessModes:
  - ReadWriteOnce
  size: 64Gi
  annotations: {}

serviceMonitor:
  additionalLabels: {}
  annotations: {}
  enabled: true
  interval: "15s"

config:
  table_manager:
    retention_deletes_enabled: true
    retention_period: "180d"
